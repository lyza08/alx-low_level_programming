#include <stdio.h>

/**
 * main - Entry point
 * Description -'print alphabets in lowercase except e and q'
 * Return: Always 0 (successful)
 */

int main(void)
{
	int x;

	for (x = 0; x <= 9; x++)
	{
		printf("%d", x);
	}
	printf("\n");
	return (0);
}
