#include <stdio.h>

/**
 * main - printing a program name
 * @argc: And count the number of arguments
 * @argv: array of strings
 * Return: Always 0 (successful)
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
